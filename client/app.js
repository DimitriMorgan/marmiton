'use strict';

var App = angular.module('MarmitonApp', []);

App.factory('GlobalFunctions', function() {
   return {
       getRecipes: function(type) {
           // type = popular | recent

       }
   }
});